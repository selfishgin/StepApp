<Window x:Class="StepAppAdmin.Views.AddCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Add Category" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--<TextBlock Text="Category Name:" VerticalAlignment="Center"/>-->
        <TextBox x:Name="CategoryNameTextBox" materialDesign:HintAssist.Hint="Category Name" VerticalAlignment="Center" Grid.Row="0" Margin="0,5,0,5"/>

        <!--<TextBlock Text="Description:" Grid.Row="1" VerticalAlignment="Center"/>-->
        <TextBox x:Name="CategoryDescriptionTextBox" materialDesign:HintAssist.Hint="Description" VerticalAlignment="Center" Grid.Row="1" Margin="0,5,0,5"/>

        <Button Content="Add Category" Grid.Row="2" HorizontalAlignment="Right" Width="100" Click="AddCategory_Click"/>
    </Grid>
</Window>
