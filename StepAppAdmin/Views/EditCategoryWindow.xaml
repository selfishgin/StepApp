<Window x:Class="StepAppAdmin.Views.EditCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Category" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Category Name:" VerticalAlignment="Center"/>
        <TextBox x:Name="CategoryNameTextBox" Grid.Row="0" Margin="0,5,0,5"/>

        <TextBlock Text="Description:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="CategoryDescriptionTextBox" Grid.Row="1" Margin="0,5,0,5"/>

        <Button Content="Save Category" Grid.Row="2" HorizontalAlignment="Right" Width="100" Click="SaveCategory_Click"/>
    </Grid>
</Window>
